- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.editorconfig:
    ~/.eslintrc.js:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.zshrc:

- shell:
    - ln -s "$(brew --prefix)/bin/gsha256sum" "$(brew --prefix)/bin/sha256sum"
